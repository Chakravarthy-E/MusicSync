const PRIMARY = '#0F0F0F';
const SECONDARY = '#337CCF';
const CONTRAST = '#FFFFFF';
const INACTIVE_CONTRAST = 'rgba(255, 255, 255, 0.5)';
const ERROR = '#FF5B14';
const SUCCESS = '#3BEF1E';
const OVERLAY = '#232D3F';

const colors = {
  PRIMARY,
  SECONDARY,
  CONTRAST,
  INACTIVE_CONTRAST,
  ERROR,
  SUCCESS,
  OVERLAY,
};

export default colors;
